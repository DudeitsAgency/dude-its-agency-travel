const $=s=>document.querySelector(s);const $$=s=>document.querySelectorAll(s);const dep=$('#departure'),ret=$('#returnDate');const today=new Date().toISOString().slice(0,10);dep.min=today;ret.min=today;$$('input[name="tripType"]').forEach(r=>r.addEventListener('change',()=>{ret.disabled=r.value==='one';ret.parentElement.parentElement.style.opacity=r.value==='one'?'.45':'1'}));$('#swapBtn').addEventListener('click',()=>{const a=$('#from'),b=$('#to'),v=a.value;a.value=b.value||'Where to?';b.value=v});$$('.deal button').forEach(b=>b.addEventListener('click',()=>{const [from,to]=b.dataset.route.split('|');$('#from').value=from;$('#to').value=to;$('#flights').scrollIntoView({behavior:'smooth',block:'center'})}));$('#flightSearch').addEventListener('submit',e=>{e.preventDefault();const note=document.querySelector('.search-note');note.innerHTML='<strong style="color:#2f8df5">Searching live options…</strong> The live-search provider can be connected here. The booking-request flow is ready.';setTimeout(()=>note.innerHTML='<strong style="color:#102b5c">Search ready.</strong> '+$('#from').value+' → '+$('#to').value+'. Choose an option and request a callback from a Dude travel advisor.',700)});$('#contactForm').addEventListener('submit',e=>{e.preventDefault();$('#formStatus').textContent='Request received — connect your CRM/email endpoint here to send this lead to the team.';e.target.reset()});$('#menuBtn').addEventListener('click',()=>document.querySelector('.main-nav').classList.toggle('mobile-open'));