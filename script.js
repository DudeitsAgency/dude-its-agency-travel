const tabs=document.querySelectorAll('.search-tabs button');const note=document.getElementById('searchNote');tabs.forEach(t=>t.addEventListener('click',()=>{tabs.forEach(x=>x.classList.remove('active'));t.classList.add('active');const type=t.dataset.type;note.textContent=type==='flight'?'Search is a starting point — a Dude travel advisor will help you secure the right option.':type==='hotel'?'Tell us your destination and dates — we’ll help match you with the right stay.':'Tell us what kind of trip you want and we’ll build the options around you.';}));document.getElementById('searchForm').addEventListener('submit',e=>{e.preventDefault();const from=document.getElementById('from').value,to=document.getElementById('to').value,date=document.getElementById('date').value,trav=document.getElementById('travelers').value;note.textContent='Got it — '+from+' → '+to+' on '+date+' for '+trav+'. A Dude travel advisor can take it from here.';document.getElementById('contact').scrollIntoView({behavior:'smooth'});});document.getElementById('contactForm').addEventListener('submit',e=>{e.preventDefault();document.getElementById('formStatus').textContent='Request received. Connect your CRM/email endpoint here to send it to the team.';e.target.reset();});